This folder carries an example hello-world .cu that needs to compile to a .ptx for use in a closed-source Nvidia tool.

This folder is not a modular example, making reference to the broader repo as a dependency, but builds as part of the repo itself
